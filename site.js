const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('#primary-nav');if(navToggle&&nav){const syncNav=()=>{if(innerWidth>760){nav.hidden=false;navToggle.setAttribute('aria-expanded','false')}else if(navToggle.getAttribute('aria-expanded')==='false')nav.hidden=true};navToggle.addEventListener('click',()=>{const open=navToggle.getAttribute('aria-expanded')==='true';navToggle.setAttribute('aria-expanded',String(!open));nav.hidden=open});nav.addEventListener('keydown',e=>{if(e.key==='Escape'){nav.hidden=true;navToggle.setAttribute('aria-expanded','false');navToggle.focus()}});addEventListener('resize',syncNav);syncNav()}
