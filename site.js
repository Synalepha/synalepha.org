const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('#primary-nav');if(navToggle&&nav){const setNav=open=>{navToggle.setAttribute('aria-expanded',String(open));navToggle.textContent=open?'Close':'Menu';nav.hidden=!open};const syncNav=()=>{if(innerWidth>760){nav.hidden=false;navToggle.setAttribute('aria-expanded','false');navToggle.textContent='Menu'}else if(navToggle.getAttribute('aria-expanded')==='false')setNav(false)};navToggle.addEventListener('click',()=>setNav(navToggle.getAttribute('aria-expanded')!=='true'));nav.addEventListener('keydown',e=>{if(e.key==='Escape'){setNav(false);navToggle.focus()}});nav.addEventListener('click',e=>{if(innerWidth<=760&&e.target.closest('a'))setNav(false)});addEventListener('resize',syncNav);syncNav()}
