import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {AppShell} from '@/components/AppShell';
export default async function Discover(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const [{data:me},{data:people}]=await Promise.all([supabase.from('profiles').select('username,display_name,avatar_url').eq('id',user.id).single(),supabase.from('profiles').select('username,display_name,bio,mood,city').eq('visibility','public').eq('discoverable',true).neq('id',user.id).order('created_at',{ascending:false}).limit(48)]);return <AppShell profile={me||{username:null,display_name:null,avatar_url:null}}><div className="directory-page"><div className="section-title"><div><p className="eyebrow">DISCOVER</p><h1>Meet the neighborhood</h1></div><span className="chronological">newest pages first · age-group safety enforced</span></div>{people?.length?<div className="people-grid">{people.map(p=><Link className="person-card" key={p.username} href={`/u/${p.username}`}><div className="avatar">{(p.display_name||p.username||'?')[0].toUpperCase()}</div><div><h2>{p.display_name||p.username}</h2><small>@{p.username}{p.city?` · ${p.city}`:''}</small><p>{p.bio||'A new page finding its voice.'}</p><span>{p.mood?`mood: ${p.mood}`:'visit page →'}</span></div></Link>)}</div>:<div className="empty"><span>✦</span><b>Your safe discovery pool is quiet.</b><p>Only discoverable public pages in your age group appear here.</p></div>}</div></AppShell>}
